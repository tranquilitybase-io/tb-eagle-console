# This workflow will run tests when a PR is performed.
# TODO: Build, deploy and tag images
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Eagle Console - CICD Workflow

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        repository: tranquilitybase-io/tb-eagle-console
        path: tb-eagle-console
        fetch-depth: 0
    - uses: nanasess/setup-chromedriver@master
      with:
        # Optional: do not specify to match Chrome's version
        chromedriver-version: '77.0.3865.40'
    - run: |
        export DISPLAY=:99
        chromedriver --url-base=/wd/hub &
        sudo Xvfb -ac :99 -screen 0 1280x1024x24 > /dev/null 2>&1 & # optional
     - name: Setup tmate session
      uses: mxschmitt/action-tmate@v2
    - name: Install dependencies and run tests
      shell: bash
      run: |
        cd ${GITHUB_WORKSPACE}/tb-eagle-console/
        curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -
        sudo apt-get install -y nodejs
        npm install
        npm run test
