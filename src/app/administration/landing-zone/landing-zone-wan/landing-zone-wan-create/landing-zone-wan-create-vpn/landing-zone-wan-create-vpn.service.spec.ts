import { TestBed } from '@angular/core/testing';

import { LandingZoneWanCreateVpnService } from './landing-zone-wan-create-vpn.service';

describe('LandingZoneWanCreateVpnService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  xit('should be created', () => {
    const service: LandingZoneWanCreateVpnService = TestBed.get(LandingZoneWanCreateVpnService);
    expect(service).toBeTruthy();
  });
});
